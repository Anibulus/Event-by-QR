<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eventwqr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_index"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_index_round"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetalleGrupo"
            android:label="@string/title_activity_detalle_grupo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EscaneoInvitado"
            android:label="Escaneo a invitados"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Invitados"
            android:label="Lista de invitados"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="Evento via QR"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>